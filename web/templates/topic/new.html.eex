<%# Anonymous function fn f -> %>
<%# topic_path - is a helper for routing.
  It's an alias for a routing path. Which always you to use the alias
  and not have the route change when name changes
 %>
<%= form_for @changeset, topic_path(@conn, :create), fn f -> %>
   <div class="form-group">
   <%= text_input f, :title, placeholder: "Title", class: "form-control" %>
   <%= error_tag f, :title %>
   </div>

   <%= submit "Save Topic", class: "btn btn-primary" %>
<% end %>